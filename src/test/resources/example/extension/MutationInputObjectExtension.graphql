type MutationObject {
  id: Long!
  name: String!
  parent: MutationObject
}

type Query {
  hello: String
}

type schema {
  query: Query
}

input MutationInputObject {
  id: Long!
  name: String
  now: DateTime
  parent: MutationInputObject
}
